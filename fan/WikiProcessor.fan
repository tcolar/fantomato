// History:
//  Jan 10 13 tcolar Creation
//

**
** WikiProcessor
** Delegate to my OLD java impl for now until a rewrite in Fantom
**
class WikiProcessor
{
  ** Convert jotwiki text to html
  native Str process(Str text)
}